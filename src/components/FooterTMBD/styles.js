import styled from "styled-components";

export const Container = styled.div`
  padding: 24px 0;
  height: 80px;
  text-align: center;
  background: linear-gradient(0deg, #0f0e0c 16%, rgba(15, 14, 12, 0) 100%);

  > p {
    opacity: 1;
    font-size: 0.6rem;
  }

  svg {
    height: 16px;
  }
`;
